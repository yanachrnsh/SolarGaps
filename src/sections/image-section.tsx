import { styles } from '../constants/style-constants';

export const ImageSection = () => {
	return <section className={`${styles.sectionLayout} ${styles.sectionImgBg} bg-background-building h-100 rounded-xl`}></section>;
};
